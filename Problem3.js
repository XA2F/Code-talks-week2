// a simple example i can think of is the following:

const thisfunction = function () {};
console.log(this);

// this will return the global object, which is not what we want. The difference is all in with the global object

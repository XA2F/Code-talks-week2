// A simple example I can think of is the following:

const thisfunction = function () {};
console.log(this);

// This will return the global object, which is not what we want. The difference is all in the global object.
